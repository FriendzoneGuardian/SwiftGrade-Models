# =============================================================
# requirements_aes.txt — Short_Answer_NLP Module (Module C)
# SwiftGrade — BERT-based AES Pipeline (Table 2.3.3)
# GPU Target: GTX 1650 (4 GB VRAM)
# =============================================================

# ── Core Data Processing ──────────────────────────────────────
pandas>=1.3.0
numpy>=1.21.0

# ── Machine Learning ──────────────────────────────────────────
scikit-learn>=1.0.0
xgboost>=1.5.0

# ── PyTorch (GPU) ─────────────────────────────────────────────
# Install via: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# (do NOT install plain 'torch' if you want CUDA support)
torch>=2.0.0
torchvision>=0.15.0

# ── Hugging Face Transformer Stack ────────────────────────────
# Table 2.3.3: BERT Base / DistilBERT / RoBERTa
transformers==4.40.0          # BERT, DistilBERT, RoBERTa model hub
tokenizers==0.19.1            # Fast tokenizer backend (stop word removal)
huggingface-hub>=0.20.0       # Model cache management

# ── Sentence-Transformers (Semantic Similarity + Rubric Scoring)
sentence-transformers==2.6.1  # all-mpnet-base-v2 / all-MiniLM-L6-v2
accelerate>=0.20.0            # multi-GPU / mixed precision inference

# ── NLP Utilities ─────────────────────────────────────────────
nltk>=3.8.0                   # Tokenizer support, stop words
spacy>=3.0.0                  # Linguistic features, POS tagging
# After install, run: python -m spacy download en_core_web_sm
textblob>=0.17.0              # Spelling check

# ── QWK Evaluation ────────────────────────────────────────────
scipy>=1.7.0                  # cohen_kappa_score dependency

# ── Interface (Gradio) ────────────────────────────────────────
gradio>=4.0.0                 # Transformer interface / demo UI

# ── Utilities ─────────────────────────────────────────────────
pyyaml>=5.4.0
tqdm>=4.60.0
python-dotenv>=1.0.0          # Environment variable management

# ── Notebooks & Visualization ────────────────────────────────
jupyter>=1.0.0
matplotlib>=3.3.0
seaborn>=0.11.0
plotly>=5.0.0
