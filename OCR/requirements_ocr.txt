# =============================================================
# requirements_ocr.txt — OCR Module B (Handwriting → Text)
# SwiftGrade — Short Answer OCR Pipeline (Table 2.3.2)
# Models: Tesseract LSTM | TrOCR Base/Small | PaddleOCR Handwriting
# =============================================================

# ── Core Data Processing ──────────────────────────────────────
pandas>=1.3.0
numpy>=1.21.0

# ── Image Preprocessing ───────────────────────────────────────
Pillow>=9.0.0                   # PIL image loading / format conversion
opencv-python>=4.6.0            # Deskew, binarization, DPI checks, CLAHE
scikit-image>=0.19.0            # Otsu threshold, morphological ops

# ── Tesseract OCR (Config A) ──────────────────────────────────
# Requires system-level Tesseract install:
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   Ubuntu:  sudo apt-get install tesseract-ocr tesseract-ocr-eng
pytesseract>=0.3.10             # Python wrapper for Tesseract binary

# ── TrOCR — Transformer-based OCR (Config B) ──────────────────
# Install via: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.40.0            # microsoft/trocr-base-handwritten
tokenizers>=0.19.1
huggingface-hub>=0.20.0

# ── PaddleOCR (Config C) ──────────────────────────────────────
# NOTE: PaddlePaddle must be installed first (CPU or GPU build).
#   CPU:  pip install paddlepaddle
#   GPU:  pip install paddlepaddle-gpu  (CUDA 11.2 or 12.x)
# paddlepaddle             # install separately per system
paddleocr>=2.7.0                # Hybrid CNN+RNN+CTC handwriting mode

# ── OCR Evaluation Metrics ────────────────────────────────────
jiwer>=3.0.0                    # WER / CER computation (MER, WIL, WIP)
editdistance>=0.6.3             # Levenshtein distance (CER fallback)

# ── Utilities ─────────────────────────────────────────────────
tqdm>=4.60.0
pyyaml>=5.4.0
python-dotenv>=1.0.0

# ── Notebooks & Visualization ─────────────────────────────────
jupyter>=1.0.0
matplotlib>=3.3.0
seaborn>=0.11.0

# ── FALLBACK: LayoutLM / Document AI Stack (Optional / Out of Scope) ──
# Uncomment below if primary OCR configs fail and spatial context is needed.
# Note: detectron2 often requires manual build or specific wheel for Windows.
# layoutparser[layoutmodels,tesseract]
# timm>=0.4.12                  # Backbone for LayoutLMv3
# albumentations>=1.0.0         # Fast image augmentation for layout
